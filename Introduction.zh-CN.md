# NEUTRINO v3（CPU）

这个扩展让 TuneLab 2.0 直接推理 NEUTRINO v3 声库。它按原版顺序运行 `t.bin`、`p.bin`、`s.bin` 和 `v.bin`，不会调用 `NEUTRINO.exe`，也没有加入 HNSEP 或其它音频后处理。

插件提供 `SHFC` 自动化曲线。它改变 `p.bin` 所看到的风格音高，再反向补偿生成的 F0，因此可以偏移音色而不会把最终旋律整体移调。
绘制后会重新合成对应范围，并刷新 TuneLab 的合成音高回显；它不会改写用户手画的音高线，而且回显经过了反向补偿，不会按填写的音分数整体上下移动。

请在 **设置 > 扩展** 的 **声库目录** 中填写路径。路径既可以是整个 NEUTRINO 文件夹，也可以是其中的 `model` 文件夹，还可以直接指向 `model\ZUNDAMON` 这样的单独声库文件夹。点 `+` 可以继续添加路径；全部留空时仍会自动查找。

音素转换只使用插件自带的词典，不会读取这些目录中 `settings\dic` 下的词典。

安装包不包含声库模型，请另外安装并遵守官方 NEUTRINO 声库的许可协议。
